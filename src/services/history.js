import { createBrowserHistory } from 'history';
// é uma lib que serve para controlar a history api do navegador, que são nossas rotas que react-router-dom utiliza.

const history = createBrowserHistory();

export default history;